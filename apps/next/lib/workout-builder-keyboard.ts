/**
 * Temporary: use the native mobile keyboard in workout builder modals
 * while debugging viewport jitter. Set to false to re-enable the custom keyboard.
 */
export const WORKOUT_BUILDER_USE_NATIVE_KEYBOARD = true
